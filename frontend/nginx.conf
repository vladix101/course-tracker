server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router: sve nepoznate putanje vrati na index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
